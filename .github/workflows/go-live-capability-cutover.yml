name: One-shot go-live capability cutover

on:
  push:
    branches:
      - codex/gridex-web-go-live-20260819

permissions:
  contents: write

jobs:
  cutover:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/gridex-web-go-live-20260819
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/go-live-capability-cutover.mjs
      - name: Persist canonical capability cutover
        run: |
          if git diff --quiet; then
            echo "No capability drift remains."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib tests
          git commit -m "fix(api): align runtime capabilities with 2026-08-19.2"
          git push origin HEAD:codex/gridex-web-go-live-20260819
