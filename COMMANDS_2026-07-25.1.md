# Gridex Web – patchsynk och verifiering 2026-07-25.1

Målprojekt:

```text
/Users/hekmath/Desktop/Projects/gridex-web
```

Patchzip:

```text
~/Downloads/gridex-web-customer-portal-api-fix-2026-07-25.1.zip
```

## 1. Packa upp och kontrollera innehållet

```bash
rm -rf /tmp/gridex-web-api-fix
mkdir -p /tmp/gridex-web-api-fix

unzip -q ~/Downloads/gridex-web-customer-portal-api-fix-2026-07-25.1.zip \
  -d /tmp/gridex-web-api-fix

find /tmp/gridex-web-api-fix -maxdepth 8 -type f | sort
```

## 2. Dry-run

```bash
rsync -avh --checksum --itemize-changes --dry-run \
  /tmp/gridex-web-api-fix/gridex-web-customer-portal-api-fix-2026-07-25.1/files/ \
  /Users/hekmath/Desktop/Projects/gridex-web/
```

## 3. Synka ändrade och nya filer

```bash
rsync -avh --checksum --itemize-changes \
  /tmp/gridex-web-api-fix/gridex-web-customer-portal-api-fix-2026-07-25.1/files/ \
  /Users/hekmath/Desktop/Projects/gridex-web/
```

Patchsynken använder inte `--delete` och rör inte `.env`, `.git`, `node_modules` eller `.next`.

## 4. Ta bort ersatta, inaktuella kontraktsartefakter

```bash
cd /Users/hekmath/Desktop/Projects/gridex-web

while IFS= read -r file; do
  [[ -z "$file" || "$file" == \#* ]] && continue
  rm -f -- "$file"
done < /tmp/gridex-web-api-fix/gridex-web-customer-portal-api-fix-2026-07-25.1/REMOVALS.txt
```

## 5. Ren installation och full verifiering

```bash
cd /Users/hekmath/Desktop/Projects/gridex-web
rm -rf node_modules .next tsconfig.tsbuildinfo
npm ci
npm run api:refresh
npm run api:contract
npm run typecheck
npm run lint
npm test
npm run build
```

`api:refresh` hämtar de kompletta aktuella OpenAPI-dokumenten, regenererar typerna och verifierar drift.

## 6. Staging-E2E

```bash
cd /Users/hekmath/Desktop/Projects/gridex-web
mkdir -p .local
cp tests/fixtures/staging-ops-flow.example.json .local/gridex-staging-e2e.json
# Fyll filen med godkända unika staginguppgifter.

GRIDEX_API_KEY='gridex_test_xxxxxxxxx' \
GRIDEX_STAGING_E2E_FIXTURE="$PWD/.local/gridex-staging-e2e.json" \
npm run test:staging:ops
```

Deploy först när samtliga kommandon ovan är gröna.
